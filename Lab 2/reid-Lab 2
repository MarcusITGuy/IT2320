#Marcus Reid IT2320
Larget Class files
<!DOCTYPE html>
<html lang="en">
<head>
   <title>Fibonacci Sequence</title> 
   <script>
       "use strict"
       var numFib = prompt("How many numbers in the sequence?"); 
	   numFib = parseFloat(numFib);
       if (numFib< 3) {
          alert("The sequence must have 3 or more numbers.");
       } else {
	      var myFib = [numFib];  // declare the array
		  myFib[0] = 0;
		  myFib[1] = 1;
		  myFib[2] = 2;
		  // fill the array
		  for (var i= 1; myFib>1;) {
		     ...
		  }
		  // write out the results
		  for () {
		     document.writeln
		  }
	   }
	   // calculate the ratio
	   var ratio = ...
	   document.write("Ratio: " + ratio);
	</script>
</head> 
<body>
   <h2>Marcus Reid - IT 2320 - Lab 2</h2>
   gee, why did this come out at the bottom?
</body> 
</html>
